# Waiting on assets

Every place in MochiBuddy that still needs a real icon, graphic, or animation. Compiled 2026-07-27 from a full sweep of the Swift source, asset catalogs, icon bundles, and docs.

How the code marks these slots:

- `PlaceholderArtIcon` (dashed frame with a photo glyph) and `PlaceholderArt.symbol` live in `MochiBuddy/CommonUI/DesignSystem/PlaceholderArt.swift`. Every render site must be replaced before release.
- `// DESIGN NOTE:` comments tag each slot in code. Grep for `DESIGN NOTE` to re-find them all.
- The Mochi mascot itself (`MochiShared/MochiPetView.swift`) is not a placeholder. It is final code-drawn art, but the commissioned pose set and animation rig below would supersede parts of it.

Legend: [ ] waiting, [~] acceptable for TestFlight but not for App Store release.

---

## 1. Mochi brand mark / wordmark — DONE (2026-07-28, Claude Design)

The flat brand mark ships as `mochi-mark.imageset` (template, tinted at runtime) — the one-color Mochi face (blob with eyes/smile knocked out), from the "MochiBuddy Brand Logo System" Claude Design project (`assets/mark-silhouette.svg`). Reusable helpers: `MochiMark` (tinted glyph view) and `MochiListRow` `iconImage`/`iconColor`.

- [x] Letters share card wordmark → mark + "MochiBuddy" Fredoka lockup in `LetterShareCard`. `LetterDetailView.swift`
- [x] Returning-user restore screen hero mark → `MochiMark` in a surface chip. `RestoreFoundView.swift`
- [x] Paywall feature hook "Your Mochi, fully alive" → `MochiMark` via `Hook.iconImage`. `PaywallBehavior.swift` / `PaywallView.swift`
- [x] Delete-account warning, "your companion" row icon → `mochi-mark` via MochiListRow `iconImage` (also dropped the hero heart-slash badge, refreshed copy). `DeleteWarnViewModel.swift`
- [x] Notification primer → mini `MochiPetView` app-icon mock (faithful to the shipped MochiPetView-based app icon). `NotificationPrimerView.swift`

`PlaceholderArt.swift` is now unused by any render site (kept as a reusable stand-in primitive).

## 2. Treat shop art (Home tab) — DONE (2026-07-28, Claude Design)

Catalog is defined in `MochiShared/TreatCatalog.swift`; assets are keyed by treat `id`. Rendered in `MochiBuddy/Home/TreatShopSheet.swift` via `TreatArtIcon` (`MochiBuddy/CommonUI/DesignSystem/TreatArt.swift`). Art from the "MochiBuddy Treat Shop Icons" Claude Design project, rasterized from the flat-vector SVGs (rsvg-convert) into `Assets.xcassets` imagesets at 1x/2x/3x. Verified in-sim at ship size. Content team may still hand-replace later; swap the imagesets, the view/call sites stay.

- [x] Pet-action art (the free "Pet Mochi" row), 28pt slot. `TreatShopSheet.swift` → `pet-mochi.imageset`
- [x] Treat: `berry` (Sweet berry), 40pt card art. → `treat-berry.imageset`
- [x] Treat: `latte` (Matcha latte), 40pt card art. → `treat-latte.imageset`
- [x] Treat: `dango` (Dango), 40pt card art. → `treat-dango.imageset`
- [x] Treat: `cupcake` (Cupcake), 40pt card art. → `treat-cupcake.imageset`

## 3. Journal moment art (roadmap #6) — DONE (2026-07-28, Claude Design)

Custom monochrome template glyphs (solid black on transparent, tinted per flavor at runtime) from the "MochiBuddy glyph icons" Claude Design project, rasterized from SVG (rsvg-convert) into `Assets.xcassets` as template imagesets at 1x/2x/3x. Moment mapping in `JournalTimeline.icon(for:)` now returns asset names; rendered at `JournalView.swift:319` (`.foregroundStyle(theme.primaryText)`). Verified in-sim (adoption glyph + both tab states).

- [x] Moment: adoption → `moment-adoption.imageset` (heart + twinkle)
- [x] Moment: anniversary → `moment-anniversary.imageset` (sparkle cluster)
- [x] Moment: streak milestone → `moment-streak.imageset` (flame)
- [x] Moment: vacation return → `moment-vacation.imageset` (8-ray sun)
- [x] Moment: list return → `moment-list-return.imageset` (u-turn arrow)
- [x] Journal tab-bar icon → `journal-filled` / `journal-outline` imagesets, filled-when-selected / outline-when-unselected, wired in `MainTabView.swift`.

## 4. App icon (5 flavors) — DONE (keeping current exports by choice)

The five `.icon` bundles contain real PNGs, exports of `MochiPetView` generated by `MochiBuddyTests/IconExportTests.swift`. Decision (2026-07-28): keep these as the shipping app icons; no commissioned replacement needed.

- [x] Default (sesame): `MochiBuddy/AppIcon.icon`. Note: ships only the single `icon-sesame-dark` layer; confirm whether light/tinted variants are intended.
- [x] Strawberry: `MochiBuddy/AppIcon-strawberry.icon`
- [x] Matcha: `MochiBuddy/AppIcon-matcha.icon`
- [x] Mango: `MochiBuddy/AppIcon-mango.icon`
- [x] Ube: `MochiBuddy/AppIcon-ube.icon`

## 5. Widget static pose set (blocks shipping, per requirements)

*mochi-requirements.md → Widgets*. The widget must render static image exports only (never a runtime rig), one set per theme flavor. Today the widget code-draws the pet via `MochiPetView` + `ImageRenderer` (`MochiWidget/MochiWidget.swift:313-328`) and swaps to real exports when the art lands (*Implementation status → Known deltas*).

- [ ] 6 mood idle poses, exported per theme
- [ ] Asleep pose (lapsed state)
- [ ] Resting pose (vacation). Currently reuses the sleeping pose (*Implementation status → Known deltas*); the brief calls for a distinct hammock / sunglasses holiday pose (*Vacation mode*)
- [ ] Every pose must read as a monochrome silhouette for the lock screen

## 6. Mascot animation commission (Rive rig)

Spec'd in *mochi-requirements.md → Art direction: animation & sound* but not built; the app currently uses code-drawn static poses with SwiftUI springs. Deliverables per the brief:

- [ ] Character rig (Rive) matching the design-system Mochi (source SVG viewBox 180x170)
- [ ] ~6 mood idle loops
- [ ] Reaction animations: pet, task-complete, fall-asleep, wake-up, receive-treat
- [ ] **Waking-Mochi onboarding beat (exploratory):** a groggy / half-awake pose and an adoption-happy beat. Neither exists in the scripted idle set, and the sequence is animation-carried, so this may want the rig rather than scripted canvases. Also needs a static equivalent for Reduce Motion (*mochi-requirements.md → Onboarding → Exploring: the waking-Mochi adoption beat*)
- [ ] Per-state ambient sound (companion deliverable in the same brief)
- [ ] Onboarding mood-animation beat depends on this commission (*Onboarding*)

## 7. Notification imagery

- [ ] Expressive Mochi-face notification attachments, intensity-capped. Gated on the pose-set commission above; attachments land with the art (*Implementation status → Known deltas*).

## 8. Launch screen

- [~] Themed launch screen. Currently the auto-generated blank one (*submission-checklist.md*).

## 9. App Store Connect deliverables

Not in-app art, but graphics still owed for submission:

- [ ] App Store screenshots
- [ ] Subscription review screenshots for both products (*submission-checklist.md*, *Implementation status → Outstanding external setup*)

## 10. Design comps still needed (not shipped assets)

The discovery batch's three comps are **done**, all built and approved 2026-07-26 across 5 flavors (prompts archived at `ProjectDocs/design/design-comp-prompts.md`):

- [x] Best Hours cards revision (`Best Hours Cards.dc.html`)
- [x] Re-time row badge (`Suggestion Badge.dc.html`)
- [x] Effort pill and menu (`Task Size Control.dc.html`)

Still open:

- [ ] **File cleanup on `Task Size Control.dc.html`:** delete the superseded food-metaphor states (1a-1c) so the file carries only the EFFORT version.
- [ ] **Editor ghost pill** — the dimmed suggested time inside the time pill, plus its reason caption and dismiss affordance. The last unbuilt discovery-batch item; design is locked but no comp exists (*mochi-requirements.md → The discovery batch → Editor layout*). Needs the dim treatment checked in all five flavors so it stays distinguishable from an empty pill.
- [ ] **Waking-Mochi onboarding sequence** — the tap-to-wake adoption beat (asleep → groggy → awake → adopted → name). Exploratory, needs a comp before it becomes a spec (*mochi-requirements.md → Onboarding → Exploring: the waking-Mochi adoption beat*).

---

## Not art, but flagged during the sweep

- `Assets.xcassets/AccentColor.colorset` is defined but has no color value, while the build setting still points at it. Either set the color or drop the setting.
- Privacy Policy, support, and EULA links in `MochiBuddy/You/MochiLinks.swift` are placeholders until the real site is live.

## Confirmed fine (no asset needed)

- No Lottie/Rive/sprite/gif references anywhere yet; all current animation is procedural SwiftUI (`MochiMotion` springs in `MochiShared/MochiTheme.swift:108-110`).
- No named-image references (`Image("...")` / `UIImage(named:)`) exist, so nothing is silently missing from the catalog.
- Fonts are complete: `Fredoka-Variable.ttf` and `Nunito-Variable.ttf` bundled and registered.
- Loading skeletons (`MochiSkeleton.swift`) and the Reminders primer mock rows are intentional UI, not pending art.
- Widget intents and buttons use SF Symbols by design.
